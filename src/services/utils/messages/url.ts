const enum ErrorForUrl {
  NOT_FOUND = 'The requested url does not exists',
  NOTHING_TO_DELETE = 'There is no url to be deleted'
}

const enum MessageForUrl {}

export { ErrorForUrl as EFU, MessageForUrl as MFU }
